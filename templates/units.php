<?php

/**
 *
 */

class T_Units
{
    // Force
    const N     = ' <div class="unit" title="Newton">[N]</div>';

    // Power
    const W     = ' <div class="unit" title="Watt">[W]</div>';

    // Energy
    const J     = ' <div class="unit" title="Joule">[J]</div>';
    const kwh   = ' <div class="unit" title="Kilowatt hour">[kwh]</div>';
    const Wh    = ' <div class="unit" title="Watt hour">[Wh]</div>';

    // Electric units
    const V     = ' <div class="unit" title="Volt">[V]</div>';
    const A     = ' <div class="unit" title="Ampere">[A]</div>';
    const Ah    = ' <div class="unit" title="Ampere hours">[Ah]</div>';

    // Other SI units
    const Kg    = ' <div class="unit" title="Kilogramm">[Kg]</div>';
    const M     = ' <div class="unit" title="Meter">[M]</div>';

    // Time units
    const Y     = ' <div class="unit" title="Year">[Y]</div>';
    const H     = ' <div class="unit" title="Hour">[H]</div>';
 
    // Currencies
    const CFA   = ' <div class="unit" title="Central African Franc">[CFA]</div>';
    //const DOL   = ' <div class="unit" title="Dollar">[$]</div>';

    // Others
    const Percent = ' <div class="unit" title="Percent">[%]</div>';
    const Cycles  = ' <div class="unit" title="Cycles">[Cycles]</div>';
    const DEG   = ' <div class="unit" title="Celsius">[&#176;C]</div>';

    // SI: Meter, Kilogramm, Second, Ampere, Kelvin, Mol, Candela
    // Joule: 1J = 1 kg * m**2 / s**2 = 1 Ws = 1 Nm
    // Volt: 1V = 1 W/A = 1 J/C = 1 N*m / (A*s) = 1 kg * m**2 / (A * s**3)
    // Watt = m**2 * kg / s**3
    // Coulomb = A*s
    // Newton = kg * m / s**2
    // Ohm = m**2 * kg / (s**3 * A**2)
    // Siemens = 1 / Ohm
    // Tesla = kg / (s**2 * A)
    // rad = 1

};

